services:
  mlflow-mcp:
    build: .
    ports:
      - "8080:8080"
    environment:
      - MLFLOW_TRACKING_URI=${MLFLOW_TRACKING_URI:-./mlruns}
      - MCP_HOST=0.0.0.0
      - MCP_PORT=8080
    volumes:
      - ./mlruns:/app/mlruns
